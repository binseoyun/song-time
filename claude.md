- 모든 의사 결정의 내용은 문서로 작성되어야 하고, 리팩토링 과정과 문제해결 방법 등 모두 문서로 작성
- 모든 문서는 doc 폴더 안에 기재됨
- 아키텍처/기술 의사 결정은 결정 1건당 ADR 파일 1개로 작성: `doc/ADR/ADR-NNN-<주제>.md` (예: `ADR-001-동시성-제어-방식-선택.md`). 번호는 순차 증가시키고, 템플릿은 상황(Context) / 검토한 대안과 트레이드오프 / 결정과 이유 / 결과(정량·정성)를 따른다
- 리팩토링 과정은 `doc/refactoring/`, 문제해결(트러블슈팅) 과정은 `doc/troubleshooting/`에 각각 문서로 세세히 작성
- 추가로 백엔드 개발자, 인프라/클라우드 개발자로서 역량을 포트폴리오에 적을 만한 내용은 `doc/portfolio/`에 작성

## 작업 기준 문서
- 모든 고도화/리팩토링 작업은 `doc/portfolio-roadmap.md`를 기준으로 진행한다. 작업 시작 전 반드시 이 문서를 읽고, 어떤 Phase/서사에 속하는 작업인지 확인한다.
- Phase 순서를 지킨다: Phase 1(테스트+시크릿 위생) → 2(동시성) → 3(K8s 운영화) → 4(CI/CD) → 5(관측성). 앞 Phase를 건너뛰고 뒤 Phase를 진행하지 않는다.
- 새 기능 추가는 하지 않는다. 기존 기능의 문제 정의 → 측정 → 개선 → 재측정만 한다.
- 모든 개선은 Before/After 정량 수치를 남긴다. 측정 원본 데이터는 `doc/experiment/`에 저장한다.
- 작업 진행 상황(완료/진행중)은 `doc/portfolio-roadmap.md`에 직접 갱신한다.
- 시크릿(API 키, 비밀번호 등)은 어떤 문서나 커밋에도 남기지 않는다.
