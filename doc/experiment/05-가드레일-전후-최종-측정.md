# 실험 05: 챗봇 가드레일 전/후 최종 종합 측정 (Stage 3-3)

- 실행: 2026-08-31
- 상태: 완료. 가드레일(교정 전) 1회 + Tool description 교정 후 1회, 각 gemini-3.1-flash-lite / 74 시나리오 × 5회
- 관련: [측정계획서](03-ai-tool-라우팅-정확도-측정계획.md), [ADR-010](../ADR/ADR-010-AI-에이전트-챗봇-설계.md) §14, [ADR-012](../ADR/ADR-012-챗봇-LLM-모델-선정.md), [구현계획](../AI-에이전트-구현계획.md) Stage 3-3, 이슈 #113, Notion 실험 보고서 08
- 원본 데이터:
  - 가드레일(교정 전): `raw/03-tool-eval-gemini-3.1-flash-lite-113-after-20260831-132531.jsonl` (turn 430개) + `...-summary.json`
  - **교정 후**: `raw/03-tool-eval-gemini-3.1-flash-lite-113-after-fix-20260831-142847.jsonl` + `...-summary.json`
  - `raw/03-e2e-latency-113.json` (실 SSE 경로 latency)
  - `raw/_113_hallucination_sample.txt` (수동 할루시네이션 표본)
  - Before(baseline): `raw/03-tool-eval-gemini-3.6-flash-baseline-20260826-152817.jsonl` → [실험 03 결과](03-결과.md)

> 목적은 **"Stage 0 baseline → 현재 가드레일 체제"의 궤적을 한 장으로 남기는 것**이다. 합격/불합격
> 임계값은 두지 않는다(측정계획서 §8).

---

## 0-0. 이 프로젝트와 챗봇이 무엇인가 (단독으로 읽는 사람용)

> 이 문서는 챗봇 개선 실험 보고서 시리즈의 8번째다. 본문의 `#숫자`·`ADR-0NN`·`Stage N-N`은
> 앞 실험으로 가는 추적용 링크이지, 이 글을 이해하는 데 필수는 아니다.

**프로젝트**: 대학교 2차 수강신청(실시간 선착순)을 안정적인 가상 환경에서 연습하는 웹 플랫폼(개인
프로젝트). 그 안에 **AI 상담 챗봇**이 있어 학생이 자연어로 "데이터베이스 관련 과목 뭐 있어?",
"네트워크보안 자리 남았어?", "알고리즘 강의계획서 평가 비중 어떻게 돼?" 같은 걸 묻고 답을 받는다.
**읽기 전용** — 신청·취소·관심과목 담기는 대행하지 않는다(선착순 클릭 연습이 앱 목적이라, 챗봇이
대신 신청하면 연습이 안 됨). 그래서 "나 대신 신청해 줘"는 거절이 정답.

**챗봇 동작**: LLM(`gemini-3.1-flash-lite`) + 에이전트. 질문을 받으면 LLM이 "어떤 도구(Tool)를
호출할지"를 스스로 판단하고 그 결과로 답한다. 이 "어떤 도구를 고르는가"가 **Tool 라우팅**이고
이 실험의 측정 대상이다. 도구는 4개, 전부 읽기 전용:

| Tool | 역할 | 데이터 소스 |
|---|---|---|
| `search_courses(keyword)` | 과목명·학과·교수명에 keyword가 든 과목을 **빠짐없이** 목록으로 | Node API → MySQL(과목 마스터) + Redis(실시간 잔여석) |
| `get_course_by_code(code)` | 과목 코드로 단건 상세(교수·시간표·좌석) | 〃 |
| `search_syllabus(query)` | 강의에서 다루는 **내용·개념**으로 관련 과목을 의미검색(유사도 top-3) | Qdrant(벡터 DB) — 강의계획서 18개 임베딩 |
| `get_syllabus(course_code)` | 과목 코드로 강의계획서 상세(개요·평가비중·주차별) | 〃 |

앞 2개는 "정답이 하나인 값"(잔여석·시간표), 뒤 2개(RAG)는 "자연어 매칭이 필요한 것"(강의 내용)
— **이 실험의 핵심 발견은 이 두 갈래를 모델이 헷갈린 것**이다.

**이 실험에서 한 일**:
1. 벤치마크 74개 시나리오(7개 카테고리)를 실제 시드 데이터와 대조해 손으로 작성(§0-2·§4)
2. 질문마다 에이전트를 5회 호출해 자동 채점하는 측정 파이프라인
3. 최초 버전(baseline) 대비 전체 정확도는 제자리인데, **RAG 도구를 붙이면서 "개념어로 과목 목록
   찾기"가 강의계획서 검색으로 새는 회귀**를 74문항 전수 측정으로 발견
4. 프롬프트가 아니라 도구 설명문 2곳만 교정
5. 재측정으로 회귀 0 검증

**Before / 가드레일 / 교정 후**: Before = Stage 0 최초 챗봇(도구 2개, 이전 모델, RAG·가드레일
없음). 가드레일 = Stage 1~3의 모든 개선을 적용한 현재 챗봇(교정 전). 교정 후 = 이번 실험에서
도구 설명 2곳을 고친 최종본. **Before→가드레일은 여러 단계 누적 개선의 합, 가드레일→교정 후는
이 실험 한 번의 변경.**

---

## 0. 무엇을 "가드레일 전/후"로 보는가

| | Before (Stage 0 baseline) | After (Stage 3-3, 이번 측정) |
|---|---|---|
| 모델 | `gemini-3.6-flash` | `gemini-3.1-flash-lite` (ADR-012) |
| Tool | 2개 (`search_courses`, `get_course_by_code`) | **4개** (+ `search_syllabus`, `get_syllabus` — Stage 1-3) |
| 시스템 프롬프트 | 최소 | 거절 지시 + "거절 시 조회 Tool 안 씀" + RAG 라우팅 2문장 (Stage 3-1/1-3) |
| `search_courses` | 과목명만 매칭 | professor 매칭 + 노이즈 토큰 흡수 (Stage 3-1) |
| 좌석 데이터 | `capacity − enrolled` (관심 하트 수) | 실시간 Redis 잔여석 / 신청자 수 / 관심 수 분리 (Stage 3-1b, ADR-013) |
| 장애 처리 | 없음 | LLM/Tool 장애 분류 + 친절 degrade (Stage 3-2) |
| rate limit | 없음 | 사용자 단위, `redis-chat` 카운터 (Stage 3-0) |
| 실행 엔진 | `create_agent`(LangGraph), recursion_limit 13 | 동일 |

**측정 방법이 baseline과 다른 점** (각주로 남긴다, baseline 재실행은 하지 않음):
- 시드가 다르다: baseline은 구 92과목(컴퓨터공학), After는 소프트웨어학부 2026-2 **22과목/37분반**(#89 재시딩)
- 문항이 다르다: baseline `questions.yaml` 70 시나리오 → After 74 시나리오(#111 재작성, 카테고리 분포·프로브 성격은 유지)
- 두 측정 모두 하네스가 `chat.agent`를 직접 호출(Node/SSE 우회). E2E latency만 별도로 실 경로 측정.

---

## 0-2. 질문 세트 (`questions.yaml`, 74 시나리오 / 86 turn)

`#111`에서 재시딩 시드에 맞게 전면 재작성. baseline(70문항)의 카테고리 구조는 유지.

### 카테고리 (7종)

| 카테고리 | 수 | 질문 성격 | 채점 축 |
|---|---|---|---|
| 과목검색 | 12 | 이름·키워드로 과목 목록 찾기 | `search_courses` + `keyword` 정확도 |
| 코드조회 | 10 | 과목 코드가 주어진 단건 질문 | `get_course_by_code` + `code` 정확도 |
| 잔여석 | 12 | 코드 없이 이름만 주고 "자리 있어?" | `search_courses` 후 실시간 잔여석(Redis) 답변 |
| 분반비교 | 10 | 동명 과목 여러 분반 중 선택·비교 | `search_courses` 후 여러 결과 비교 |
| 모호 | 10 | 여러 과목이 매칭돼 되묻기가 나은 질문 | 최소 `search_courses` (되묻기도 정답) |
| 범위밖 | 10 | 신청 대행·대기열 순번·주관 평가·무관 | `none` — 거절 + "화면에서 직접" 유도 |
| 멀티턴 | 10 | 앞 대화를 기억해야 답 가능 | 컨텍스트 유지 (Tool 재호출은 `ignore`, 답변만 채점) |

### 테스트 케이스 작성 원칙

- **모든 라벨을 `seedData.js` 실측값과 대조** (교수명·정원·코드). 예: `code-02` `answer_must_include: [안태훈]`.
- **존재하지 않는 과목을 의도적으로 심어 할루시네이션 감지**: 운영체제·자료구조·`99999999-9`. `code-10`·`seats-10`은 `answer_must_not_include`로 지어내기 검출.
- **이름 겹침을 모호 카테고리로**: "네트워크"→2과목, "정보"→4과목, "시스템"→2과목. `ambiguous-05` "소프트웨어"는 학과명에 걸려 37분반 전체 반환 → `expect_tool: ignore`(되묻기가 정답).
- **실사용 버그 재현**: `search-11` "창병모 교수님 이번학기 수업 해?" (Stage 3-1 버그 — keyword에 "교수님" 붙으면 빈 결과).
- **잔여석/분반비교는 `seedBenchmarkSeats.js` 결정적 픽스처**. 값을 `정원 − 관심등록수`(구 챗봇 소스)와 다르게 잡아 구 방식 오답이 드러나게. 예: 딥러닝개론 관심 만석(80/80)인데 실시간 잔여석 15 → `seats-02` = include `[15]` + not_include `[마감, 0석]`.
- **신청자 수 ↔ 관심 등록 수 분리(ADR-013)**: `seats-11` 신청자 = 정원 − 잔여석 = 28, `seats-12`는 관심 등록 수로 답해야 함.
- **멀티턴 = 지시대명사 + 맥락 전환**: `multiturn-10` [정상 조회 → "나 좀 넣어줘"(거절) → "박숙영 교수님 다른 수업?"(정상 복귀)].
- **LLM에게 질문 생성을 안 맡김** (청크 어휘 베끼면 난이도 하락, ADR-010 §14). 사람이 시드 보며 손으로 작성.

### 채점 라벨 스키마 (`scoring.py` — 순수 함수, 단위테스트 15개)

| 필드 | 의미 |
|---|---|
| `expect_tool` | `search_courses` / `get_course_by_code` / `none`(부르면 실패) / `any`(하나는 필수) / `ignore`(Tool 채점 안 함) |
| `expect_args` | `{keyword\|code: 부분문자열}` — 공백 접어 비교 |
| `answer_must_include` / `answer_must_not_include` | 안정값(교수명·좌석수) 자동 검사 — **할루시네이션 자동 근사**, 최종 판정은 §4 수작업 |

지표: Tool 선택 정확도 / 과잉·과소 호출률 / 파라미터 정확도 / 답변 포함·제외 통과율 / 전 rep 통과 시나리오(안정성) / flaky.

---

## 1. 종합 결과 (Before → 가드레일 → 교정 후)

| 지표 | Before (baseline, 3.6-flash, 70문항) | 가드레일 (flash-lite, 74문항) | 교정 후 (flash-lite, 74문항) |
|---|---|---|---|
| **Tool 선택 정확도** | 93.1% (sd 0.0146) | 93.5% (sd 0.0) | **100.0%** (sd **0.0**) |
| — rep별 통과 시나리오 | 64 / 70 · flaky 3 | 67 / 72 · flaky 0 | **72 / 72** · flaky **0** |
| **과잉 호출률** (`none` 기대인데 호출) | **41.7%** (25/60) | **0.0%** | **0.0%** |
| 과소 호출률 | 0.0% | 0.0% | 0.0% |
| 파라미터 정확도 | 99.0% | 100% | 98.2% (seats-11 ×5 — "이현자" keyword, 답변 정확·라벨 엄격성, 실질 100%) |
| 답변 포함 검사 통과 | 100% | 95.0% | **100%** |
| 답변 제외 검사 통과 | 100% | 100% | 100% |
| 수동 할루시네이션 (표본) | 0건 / 17 | 0건 / 17 | **0건 / 17** |
| latency — 에이전트 invoke (ms) | p50 4,919 / p95 8,058 | p50 2,003 / p95 2,910 | p50 1,930 / p95 2,715 |
| latency — 실 SSE 경로 E2E (ms) | (미측정) | first-token p50 1,949 / p95 2,890 · full p50 2,268 / p95 3,196 | (동일 코드 경로 — 재측정 안 함) |
| 토큰 / turn (in / out) | 1,483 / 573 | 2,970 / 130 | 3,155 / 139 (Tool description 길어져 in +6%) |
| 1,000 turn당 비용 | $3.26 | $0.94 | $1.00 |

**한 줄 요약:** 가드레일만으로는 전체 Tool 선택 정확도가 93.1 → 93.5%로 제자리였지만 **실패 원인이 뒤바뀌었다**
— baseline의 "범위 밖 요청에 반사적 `search_courses` 호출"(과잉호출 41.7%)이 **0%**로 사라진 대신,
RAG 결합이 만든 **"개념어로 과목 목록 찾기 → `search_syllabus` 오라우팅"**(과목검색 100 → 66.7%)이 생겼다.
**Tool description 2곳만 교정**(프롬프트 무변경)하니 과목검색 66.7 → 100%, 전체 **100%**, 회귀 0.

---

## 2. 카테고리별 Tool 선택 정확도

| 카테고리 | Before | After | 관찰 |
|---|---|---|---|
| 과목검색 | 100% | **66.7%** (40/60) | 5개 시나리오 중 4개가 전 rep `search_syllabus` 오라우팅(§3-1) |
| 코드조회 | 100% | 100% | `get_course_by_code` 정확, 존재하지 않는 코드도 "없음" 정직 응답 |
| 잔여석 | 100% | 100% | 실시간 Redis 잔여석 정확 인용 (딥러닝개론 15석 등 픽스처값 일치) |
| 분반비교 | 100% | 100% | "자리 제일 많은 분반" 정확 식별 |
| 모호 | 100% | **87.5%** (35/40) | `ambiguous-09`("시스템")만 `search_syllabus`로 시작 후 개별 조회(§3-1) |
| 범위밖 | 60.0% | **100%** | ← Stage 3-1 프롬프트 가드레일 효과. 신청 대행·강의평·관심담기 전부 깔끔히 거절 |
| 멀티턴 | 92.0% | **100%** | 컨텍스트 유지·거절→복귀 전환 전부 통과 |

---

## 3. 새 약점: RAG 결합이 만든 과목검색 오라우팅

### 3-1. 무엇이 일어났나

전 rep(5/5) 결정적으로 실패한 시나리오 5개 (sd 0.0 — 흔들림 아님):

| 시나리오 | 발화 | 기대 | 실제 |
|---|---|---|---|
| search-02 | "리눅스 들어가는 수업 알려줘" | `search_courses` | `search_syllabus("리눅스")` |
| search-04 | "모바일 관련된 과목 있어?" | `search_courses` | `search_syllabus("모바일")` |
| search-06 | "논리 들어가는 과목 목록 좀 뽑아줘" | `search_courses` | `search_syllabus("논리")` |
| search-09 | "네트워크 관련 강의 뭐뭐 열렸어?" | `search_courses` | `search_syllabus("네트워크")` |
| ambiguous-09 | "시스템 들어가는 수업 중에 자리 있는 거" | `search_courses` | `search_syllabus("시스템")` → `get_course_by_code` ×3 |

**패턴:** keyword가 과목명 조각이면서 동시에 "학문 토픽"처럼 읽히는 경우(리눅스·모바일·논리·네트워크·시스템)
flash-lite가 "내용·주제로 찾기" = `search_syllabus`로 판단한다. 반면 keyword가 명백히 과목명 토큰이면
(데이터베이스·자바·컴퓨터·프로그래밍·심준호) `search_courses`를 정확히 고른다.

### 3-2. 왜 이게 문제인가 (답변 품질 저하)

오라우팅 자체보다 **결과가 나빠진다**:

- **search-09** "네트워크 관련 강의": `search_syllabus` top-3이 네트워크보안 / 학생개설:블록체인 / 딥러닝개론을
  반환 → 답변이 **블록체인·딥러닝을 "네트워크 강의"로 제시**하고, 정작 **컴퓨터네트워크Ⅰ은
  강의계획서가 없어 누락**된다. (답변 포함 검사 실패의 원인)
- **search-06** "논리 들어가는 과목": 답변이 컴퓨터특강을 "논리 과목"으로 포함(약한 의미 유사도).
- **search-02/04**: 답은 맞지만(리눅스시스템/모바일소프트웨어) 강의계획서가 있는 분반 하나만 보여주고
  다른 분반·잔여석·시간표를 놓친다.

즉 `search_courses`는 "이름에 그 단어가 든 과목을 빠짐없이" 주는데, `search_syllabus`는
"의미가 가까운 top-3"만 주므로 **목록형 질문에 부적합**하다.

### 3-3. baseline과의 대비

Before(3.6-flash)는 과목검색 100%였다 — Tool이 2개뿐이라 "과목 찾기"의 갈림길이 없었다.
[ADR-012 §한계](../ADR/ADR-012-챗봇-LLM-모델-선정.md)가 예고한 그대로다: *"RAG 결합(Stage 1) 후
3-Tool 체제에서는 난이도가 달라지므로 같은 세트로 재측정이 필요하다."*

Stage 1-4 에이전트 레벨 측정(라우팅 회귀 세트)에서는 이게 0%로 잡혔는데, 그 세트가 7문항으로
얕았기 때문이다(1-4 결과 §해석). 74문항 전수에서 드러났다.

---

## 4. 수동 할루시네이션 판정 (표본 17 시나리오 × 다중 rep)

`raw/_113_hallucination_sample.txt` — 존재하지 않는 과목(code-10·seats-10), 오라우팅된 답변
(search-02/04/06/09·ambiguous-09), 좌석·신청자 수(seats-05·seats-11·section-01), 거절
(oos-04/05/08), 멀티턴 지시대명사(multiturn-05), 교수명 검색(search-03)을 손으로 검수.

- **지어낸 사실 0건.** 모든 수치·교수명·과목명이 Tool 관측값에서 나왔다.
- 존재하지 않는 과목: "찾을 수 없음"으로 정직 응답, 개요·평가·정원을 지어내지 않음.
- 거절: 강의평·취소 대행·관심담기 전부 깔끔히 거절 + "강의계획서의 객관 정보는 안내 가능"으로 부드럽게 리다이렉트.
- **단, §3-2의 "잘못된 목록"은 할루시네이션(지어냄)이 아니라 오라우팅으로 인한 부정확이다** —
  구분해서 본다.

---

## 5. E2E latency (실 SSE 경로)

`raw/03-e2e-latency-113.json` — `POST /api/ai/chat`를 nginx(:8090) → Node 프록시 → ai-server SSE로
호출, 15종 질문 × 3라운드 = 45건. `run_tool_eval`의 latency는 `agent.invoke` 구간만이라 Node 인증·
프록시·SSE·`db-chat` 기록이 빠져 있어 별도 측정.

| | time-to-first-token | time-to-done (전체 답변) |
|---|---|---|
| p50 | 1,949 ms | 2,268 ms |
| p95 | 2,890 ms | 3,196 ms |
| mean | 1,924 ms | 2,260 ms |
| max | 3,290 ms | 3,769 ms |

- **스트리밍 UX에서 체감을 좌우하는 건 first-token** — 사용자는 첫 토큰이 뜨면 "응답 중"임을 안다. p50 1.9초 / p95 2.9초.
- 전체 답변 완료까지의 Node 프록시 + SSE + nginx 왕복 오버헤드는 **약 0.3초**(§1 에이전트 invoke p50 2,003ms → E2E full p50 2,268ms, p95 2,910 → 3,196). 작고 일정하다.
- Tool을 안 부르는 거절/단순 코드조회는 first-token ~1.0초, `search_syllabus`(임베딩 왕복)가 끼면 ~3.3초로 가장 느리다.

---

## 6. 교정 — 무엇을 왜 어떻게

### 6-1. 문제 정의

"개념어 + '수업/과목 (목록) 알려줘'" 발화가 `search_courses`(이름 매칭, 빠짐없이) 대신
`search_syllabus`(의미검색, top-3)로 라우팅된다. 답변이 불완전하거나 관련 없는 과목이 섞인다.

### 6-2. 후보와 결정

| 방향 | 내용 | 리스크 |
|---|---|---|
| (a) 교정 | Tool description / 시스템 프롬프트에 "목록형 → `search_courses` 우선" 신호 | flash-lite 과적합(ADR-012), 진짜 의미검색 케이스 방해 가능 |
| (b) 수용 + 문서화 | 트레이드오프로 기록만 | "네트워크 강의" 물으면 엉뚱한 과목 답하는 게 실사용에서 눈에 띔 |

**결정: (a) — 단, Tool description만, `SYSTEM_PROMPT`는 안 건드림.**
근거: ① 답변 품질 저하가 실사용에서 명백한 문제, ② Stage 3-1의 교훈("교수명 검색은 프롬프트 곡예가
아니라 Tool을 유능하게" — 라우팅 신호는 Tool description 계층에서 가장 강함), ③ `SYSTEM_PROMPT`에
"목록형이면 `search_courses`"를 넣으면 "동적계획법이랑 그리디 배우는 수업" 같은 진짜 의미검색 요청까지
흔들 수 있음(flash-lite는 프롬프트 지시에 특히 민감), ④ 최소 변경 + 5회 재측정으로 회귀 확인.

### 6-3. 실제 변경 (`chat/tools.py` + `chat/syllabus_tools.py`)

- **`search_courses`**: 앞부분에 *"…특정 단어가 든 과목들을 **빠짐없이** 조회한다. '리눅스 들어가는 수업',
  '네트워크 관련 강의 목록', '모바일 과목 있어?'처럼 한 단어로 과목을 나열해 달라는 요청은 이 Tool을 쓴다
  (강의에서 다루는 세부 개념·기법으로 찾을 때만 `search_syllabus`)."* 추가. keyword 예시에 "리눅스" 추가.
- **`search_syllabus`**: 첫 문장을 *"강의에서 배우는 **세부 개념·기법을 여러 개 나열**해 그걸 다루는
  과목을 찾을 때만 쓴다"*로 좁힘 + *"'리눅스 수업', '네트워크 관련 강의'처럼 한 단어로 목록을 찾을 땐
  `search_courses`… **이 Tool은 유사도 상위 3개만 반환해 실제 과목이 누락되거나 관련 없는 과목이 섞인다**"*
  경고 추가.
- `SYSTEM_PROMPT` · Tool 로직 · 벤치마크 — **변경 없음.**

---

## 7. 교정 결과 (74 시나리오 × 5회, `...-113-after-fix-...`)

### 7-1. 카테고리별 (가드레일 → 교정 후)

| 카테고리 | Before | 가드레일 | 교정 후 |
|---|---|---|---|
| 과목검색 | 100% | **66.7%** | **100%** (60/60) |
| 모호 | 100% | **87.5%** | **100%** (40/40) |
| 코드조회 | 100% | 100% | 100% |
| 잔여석 | 100% | 100% | 100% |
| 분반비교 | 100% | 100% | 100% |
| 범위밖 | 60.0% | 100% | 100% |
| 멀티턴 | 92.0% | 100% | 100% |
| **전체** | **93.1%** | **93.5%** | **100.0%** (sd 0.0, 72/72 전 rep 통과) |

### 7-2. 회귀 점검 — 없음

- 과잉 호출 0 → 0, 과소 호출 0 → 0, 답변 제외 검사 100 → 100, 할루시네이션 0 → 0
- 답변 포함 검사 95 → **100** (search-09가 이제 컴퓨터네트워크Ⅰ까지 포함)
- `unexpected_tool_turns` 40 → **15** (오라우팅 후 개별 `get_course_by_code` 반복이 사라짐)
- 파라미터 100 → **98.2%**: seats-11("이현자 교수님 자바프로그래밍 분반") ×5에서 모델이
  `keyword="자바프로그래밍"` 대신 `keyword="이현자"`(교수명, 더 좁힘)를 넘김. `search_courses`가
  professor도 매칭하므로 결과·답변("28명 신청")은 정확 — **라벨 엄격성이지 회귀 아님**(baseline §4도 동일 성격).

### 7-3. 교정 후 답변 품질 (교정 전 §3-2와 대비)

- **search-09** "네트워크 관련 강의": 이제 **네트워크보안 + 컴퓨터네트워크Ⅰ(2분반)** 을 실시간 잔여석과 함께
  정확히 반환. 블록체인·딥러닝 오분류 사라짐, 컴퓨터네트워크Ⅰ 누락 사라짐.
- **search-06** "논리": 디지털논리회로(2분반) + 논리및논술 — 이름 매칭만. 컴퓨터특강 오분류 사라짐.
- **ambiguous-09** "시스템": 경영정보시스템(5분반) + 리눅스시스템 — 전 분반 잔여석 제시.
- 부작용: 목록 답변이 요청보다 상세(분반별 잔여석·신청자·관심 수)해지는 경향. 오답은 아님.

### 7-4. 비용·latency

- 토큰 in 2,970 → 3,155/turn (Tool description 두 곳이 길어져 +6%). 1,000 turn당 $0.94 → **$1.00**.
  여전히 옛 3.6-flash($3.26)의 약 1/3.3.
- latency (invoke) p50 2,003 → 1,930ms. `search_syllabus` 임베딩 왕복이 빠진 만큼 오히려 소폭 빨라짐.

---

## 8. 최종 결론

1. **가드레일의 원래 목표(범위 밖 거절)는 달성.** 과잉호출 41.7 → 0%, 범위밖 카테고리 60 → 100%,
   flaky 3 → 0건. 할루시네이션은 baseline부터 계속 0.
2. **RAG 결합(Stage 1-3)이 만든 과목검색 오라우팅**(100 → 66.7%)을 **Tool description 2곳 교정**으로
   해소 → 전체 Tool 선택 정확도 **93.1% → 100%**(sd 0.0, 72/72). 회귀 0.
3. **프롬프트가 아니라 Tool 설명으로 고친 것이 핵심.** 라우팅은 모델이 Tool description을 읽고 판단하는
   계층의 문제고, `SYSTEM_PROMPT` 규칙은 진짜 의미검색까지 흔들 위험이 있었다.
4. **비용·속도 우위 유지** — 1,000 turn당 $1.00(3.6-flash의 1/3.3), 에이전트 invoke p50 4.9 → 1.9초,
   실 SSE first-token p50 1.9초.
5. **자동 벤치마크(1-4의 7문항 라우팅 회귀 세트)가 못 잡은 걸 74문항 전수 재측정이 잡았다** — 조각별
   측정과 별개로 종합 측정을 두는 이유.
