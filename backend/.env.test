# 통합 테스트 전용 환경변수 (docker-compose.test.yml의 일회용 MySQL과 짝)
# 실제 시크릿이 아니므로 커밋해도 된다.
NODE_ENV=test
DB_HOST=127.0.0.1
DB_PORT=3311
DB_NAME=sugang_test
DB_USER=test
DB_PASSWORD=test
JWT_SECRET=integration-test-secret
