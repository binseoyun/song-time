# Backend 로컬 개발용 환경변수 템플릿
# 이 파일을 `.env`로 복사한 뒤 실제 값을 채우세요. (.env는 절대 커밋 금지)

DB_HOST=127.0.0.1
DB_PORT=3310
DB_NAME=cloudsystem_db
DB_USER=
DB_PASSWORD=
NODE_ENV=development
PORT=8000

# 예: openssl rand -base64 32 로 생성
JWT_SECRET=

GEMINI_API_KEY=
