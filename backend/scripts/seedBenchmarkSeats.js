/**
 * AI 챗봇 Tool 라우팅 평가(backend/ai-server/eval)용 결정적 좌석 픽스처.
 *
 * 평가 하네스는 실제 Node API → Redis `class:{id}:seats`를 그대로 읽는다(ADR-013).
 * 그런데 실사용 Redis 상태는 "실시간 수강신청 연습" 탭 사용량에 따라 들쭉날쭉하고,
 * `seedAllClassSeats.js` 직후엔 대부분 과목이 정원=잔여석(등록이 거의 없음)이라
 * "잔여석/분반비교" 시나리오가 변별력을 잃는다.
 *
 * 이 스크립트는 questions.yaml의 잔여석/분반비교 시나리오가 참조하는 과목들에
 * 고정된 좌석 값을 심는다. 값은 의도적으로 `capacity - enrolled`(구 챗봇이 잘못
 * 쓰던 소스)와 다르게 잡아, Before/After 측정에서 "구 방식은 틀린 좌석을 답한다"가
 * 드러나도록 했다.
 *
 * 실행 (docker-compose 스택이 떠 있어야 함):
 *   docker compose exec backend_1 node scripts/seedBenchmarkSeats.js
 *
 * 멱등하다. 평가 재현 시 seedAllClassSeats.js → 이 스크립트 순으로 실행한다.
 * 픽스처에 없는 과목은 건드리지 않는다.
 */
const redis = require('../src/config/redis');
const { classSeatsKey } = require('../src/utils/redisKeys');

// code -> 실시간 잔여석. 주석은 (정원, capacity-enrolled=구 챗봇이 답하던 값).
// 시드: seedData.js 소프트웨어학부 2026-2 (22과목/37분반, #89). questions.yaml 잔여석/
// 분반비교 시나리오와 note의 결론(최다 분반 등)이 이 값에 묶여 있다 — 같이 고쳐야 한다.
const FIXTURE = {
  // 잔여석 단건 — enrolled(관심 등록)가 정원 만석인 과목은 구 챗봇이 "마감"이라 답했음
  '21003917-1': 12,  // 소프트웨어의이해       (80, 30)
  '21105625-1': 15,  // 딥러닝개론            (80, 0)   ← 구 방식은 "마감/0석"
  '21105589-1': 10,  // 인공지능산업체특강     (40, 0)   ← 구 방식은 "마감"
  '21000555-1': 23,  // 소프트웨어공학        (70, 0)   ← 구 방식은 "마감"
  '21003757-1': 8,   // 모바일소프트웨어       (40, 15)
  '21105803-1': 33,  // 데이터사이언스응용     (40, 16)
  '21003761-1': 27,  // 컴퓨터특강            (50, 31)
  '21003187-1': 25,  // 영상정보처리          (50, 14)
  '21002031-1': 17,  // 네트워크보안          (83, 2)   ← 구 방식은 "거의 마감"
  // 분반비교 세트 — note의 "자리 제일 많은 분반" 결론이 유지되도록 잡음
  '21003183-1': 40,  // 데이터베이스설계와질의 1  (60, 15)  → 최다
  '21003183-2': 3,   // 데이터베이스설계와질의 2  (60, 0)
  '21003183-3': 22,  // 데이터베이스설계와질의 3  (50, 14)
  '21000557-1': 25,  // 자바프로그래밍 1(박숙영)  (40, 19)  → 최다
  '21000557-2': 4,   // 자바프로그래밍 2(박숙영)  (40, 0)   → 신청자 36
  '21000557-3': 12,  // 자바프로그래밍 3(이현자)  (40, 0)   → 신청자 28
  '21002147-1': 30,  // 컴퓨터수학 1(채희준)     (80, 37)  → 최다
  '21002147-2': 12,  // 컴퓨터수학 2(김선필)     (80, 48)
  '21002147-3': 4,   // 컴퓨터수학 3(최영우)     (95, 2)
  '21001083-1': 8,   // 경영정보시스템 1(서보밀)  (50, 28)
  '21001083-2': 30,  // 경영정보시스템 2(한은정)  (50, 25)  → 최다
  '21001083-3': 2,   // 경영정보시스템 3(서보밀)  (50, 9)
  '21001083-4': 15,  // 경영정보시스템 4(한은정)  (50, 0)
  '21001083-5': 22,  // 경영정보시스템 5(한은정)  (50, 26)
  '21003186-1': 5,   // 컴퓨터네트워크Ⅰ 1        (70, 21)
  '21003186-2': 28,  // 컴퓨터네트워크Ⅰ 2        (70, 47)  → 최다
  '21001713-1': 6,   // 리눅스시스템 1          (70, 2)
  '21001713-2': 35,  // 리눅스시스템 2          (70, 38)  → 최다
  '21001710-1': 18,  // 디지털논리회로 1         (50, 0)   → 최다
  '21001710-2': 3,   // 디지털논리회로 2         (60, 0)
  '21000549-1': 15,  // 알고리즘 1              (100, 16) → 동률
  '21000549-2': 15,  // 알고리즘 2              (90, 0)   → 동률
  '21002144-1': 9,   // 프로그래밍개론 1(오후)   (75, 0)
  '21002144-2': 40,  // 프로그래밍개론 2(오전)   (70, 0)   → 최다
};

async function main() {
  for (const [code, seats] of Object.entries(FIXTURE)) {
    await redis.set(classSeatsKey(code), seats);
    console.log(`✓ ${code}: 잔여석 ${seats}`);
  }
  console.log(`\n픽스처 ${Object.keys(FIXTURE).length}개 과목 반영 완료.`);
  redis.disconnect();
}

main().catch((error) => {
  console.error('벤치마크 좌석 픽스처 실패:', error);
  process.exitCode = 1;
});
